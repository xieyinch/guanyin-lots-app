# 观音灵签抽签应用 - 设计文档

## 应用概述

**观音灵签抽签**是一款基于观音灵签传统文化的移动应用，用户可以随机抽取100支灵签中的任意一支，获得签文解读和人生指引。

## 屏幕列表

### 1. 首页 (Home Screen)
- **主要内容**：大型"抽签"按钮、最近一次抽签结果预览
- **功能**：
  - 点击"抽签"按钮触发抽签动画
  - 显示当前抽到的签号和签名
  - 快速查看签文预览

### 2. 签文详情屏幕 (Lot Detail Screen)
- **主要内容**：完整的签文信息展示
- **功能**：
  - 显示签号、签名、吉凶等级
  - 完整签诗文本
  - 诗意解读
  - 详细解曰
  - 仙机（各类事项指引）
  - 典故背景故事
  - 收藏/分享按钮

### 3. 历史记录屏幕 (History Screen)
- **主要内容**：用户抽签历史列表
- **功能**：
  - 按时间倒序显示所有抽签记录
  - 每条记录显示：签号、签名、吉凶等级、抽签时间
  - 点击记录可查看完整详情
  - 清空历史记录功能

### 4. 收藏屏幕 (Favorites Screen)
- **主要内容**：用户收藏的签位列表
- **功能**：
  - 显示所有收藏的签位
  - 快速访问收藏的签文
  - 取消收藏功能

## 主要用户流程

### 流程 1：首次使用 - 抽签体验
1. 用户打开应用 → 首页
2. 点击"抽签"按钮 → 触发转盘/骰子动画
3. 动画结束 → 显示随机抽到的签号
4. 自动跳转到签文详情屏幕
5. 用户查看完整签文信息

### 流程 2：查看历史记录
1. 用户点击底部标签栏"历史"
2. 显示所有历史抽签记录
3. 点击某条记录 → 查看该签文详情
4. 返回历史列表

### 流程 3：收藏签文
1. 在签文详情屏幕中
2. 点击"收藏"按钮（心形图标）
3. 签文被添加到收藏列表
4. 点击底部标签栏"收藏"可查看所有收藏

## 颜色方案

基于传统中国文化和观音灵签的意象设计：

| 颜色 | 用途 | 十六进制值 |
|------|------|----------|
| 金色 | 主要强调色、按钮、吉签 | #D4AF37 |
| 深紫 | 主背景、文本、品牌色 | #2C1B47 |
| 白色 | 卡片背景、文本 | #FFFFFF |
| 浅紫 | 次要背景、分隔线 | #E8DFF5 |
| 红色 | 警告、下签标记 | #E74C3C |
| 绿色 | 成功、上签标记 | #27AE60 |
| 灰色 | 中签标记、禁用状态 | #95A5A6 |

## 关键设计特性

### 1. 抽签动画
- 转盘旋转或骰子滚动动画
- 持续 1-2 秒，最后逐渐减速
- 显示签号变化过程，最后停在随机签号

### 2. 签文卡片设计
- 卡片式布局，易于阅读
- 根据吉凶等级显示不同背景色
- 清晰的内容分层：签号 → 签名 → 签诗 → 解读

### 3. 一手操作友好
- 所有关键按钮位于屏幕下半部分
- 快速访问常用功能（抽签、历史、收藏）
- 滑动手势返回上一屏

### 4. 深色模式支持
- 应用支持系统深色模式
- 自动适配用户设置

## 数据结构

每个灵签包含以下信息：
```
{
  id: 1-100,
  name: "签名",
  grade: "上签/中签/下签",
  poem: "签诗文本",
  meaning: "诗意解读",
  interpretation: "解曰",
  details: "仙机（各类事项指引）",
  story: "典故背景"
}
```

## 本地存储

- **历史记录**：使用 AsyncStorage 存储最近 50 条抽签记录
- **收藏列表**：存储用户收藏的签位 ID 列表
- **应用设置**：存储用户偏好设置（深色模式、通知等）

## 性能考虑

- 预加载所有 100 个签位数据到内存
- 使用 FlatList 优化历史记录列表渲染
- 动画使用 Reanimated 库确保流畅度
